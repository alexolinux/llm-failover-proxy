// Merge this into your opencode.json (global: ~/.config/opencode/opencode.json,
// or project-local ./opencode.json). opencode now talks to ONE model,
// "llm-free-pool/opencode-main" — the proxy handles switching underneath it.
{
  "$schema": "https://opencode.ai/config.json",
  "model": "llm-failover-proxy/opencode-main",
  "provider": {
    "llm-failover-proxy": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "LLM Free Proxy",
      "options": {
        "baseURL": "http://127.0.0.1:4000/v1",
        "apiKey": "{env:LITELLM_MASTER_KEY}" //Add/Replace your variable LITELLM_MASTER_KEY
      },
      "models": {
        "opencode-main": {
          "name": "llm-failover-proxy",
          "capabilities": {
            "tools": true,
            "input": ["text"],
            "output": ["text"]
          },
          "limit": {
            "context": 65536,
            "output": 8192
          }
        }
      }
    }
  }
}

